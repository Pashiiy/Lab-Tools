# Build resources

Place platform-specific assets here (e.g. installer backgrounds, license files).

Electron-builder `directories.buildResources` points to this folder.
Application icons are generated into `public/` via `npm run generate-icons`.
